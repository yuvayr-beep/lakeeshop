import React from 'react';
import StockLedgerClient from './components/StockLedgerClient';

export const metadata = {
  title: 'Stock Ledger Report | Admin',
};

export default function StockLedgerPage() {
  return <StockLedgerClient />;
}
